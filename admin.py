import db
from utils import write_msg

# Функционал администратора: просмотр заявок, изменение статуса.
# Для заглушки:
def handle_admin(user_id, message):
    write_msg(user_id, "Админ-панель пока не реализована.")